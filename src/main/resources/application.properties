spring.application.name=findadate

# The design is mobile-first and every screen is one column, so the client needs
# no eager route bundles beyond the one it lands on.
vaadin.launch-browser=false

logging.level.io.binarycodes.findadate=DEBUG
