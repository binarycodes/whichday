# The profile every Spring test names. It carries nothing this tier needs
# disabled — there is no datasource and no identity provider to keep off the
# network — and exists so that a test never silently inherits the main
# configuration when one arrives.
logging.level.io.binarycodes.findadate=WARN
