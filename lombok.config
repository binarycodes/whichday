# JaCoCo drops members carrying an annotation named *Generated, and Lombok only emits
# one when it is asked to. Without this, generated accessors count as uncovered
# instructions in exactly the two packages the 80% gate measures.
config.stopBubbling = true
lombok.addLombokGeneratedAnnotation = true
